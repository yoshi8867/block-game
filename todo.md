# 진행 상황 · 남은 작업

> 이 파일은 대화가 compact 된 뒤에도 바로 이어서 작업할 수 있도록 쓴 인수인계 문서다.
> 원작 분석 수치(레이아웃 비율·색상·점수 공식)는 전부 [docs/analysis.md](docs/analysis.md) 에 있다.

---

## 0. 프로젝트 개요

원작 모바일 게임(8×8 블록 퍼즐)을 HTML5로 모사하는 개발 연습.
원작 플레이 영상을 프레임 단위로 분석해 레이아웃·색상·점수 공식을 복원했다.

| | |
|---|---|
| 저장소 | `C:\Users\nutel\OneDrive\Desktop\dev\block-game` |
| 배포 주소 | https://yoshi8867.github.io/block-game/ |
| 분석 작업실 | `C:\Users\nutel\OneDrive\Desktop\dev\hgame` (원본 영상·wav 원본·`docs/bg_source.png`) |
| 원본 영상 | `hgame/KakaoTalk_20260817_025744434.mp4` (1080×2340, 106.7초) |
| 원본 영상 2 | `hgame/KakaoTalk_20260817_032722265.mp4` — 시작 카운트다운이 온전히 들어 있다 |

빌드 도구 없는 순수 정적 파일. ES 모듈이 아니라 일반 `<script>` 를 쓴다
(`file://` 에서 더블클릭으로 열어도 동작해야 하므로).

---

## 1. 진행 현황

| # | 단계 | 상태 |
|---|---|---|
| 1 | 뼈대 + 레이아웃 | ✅ 완료 |
| 2 | 블록 35종 + 트레이 렌더 | ✅ 완료 |
| 3 | 드래그 & 배치 판정 | ✅ 완료 |
| 4 | 점수 · NICE/PERFECT · 파편 연출 | ✅ 완료 |
| 5 | 타이머 · 인트로 카운트다운 · 경고 깜빡임 · 종료 | ✅ 완료 |
| 6 | 오디오 전체 연결 | ✅ 완료 |
| 7 | block change (1.5초 대기) | ✅ 완료 |
| — | GitHub Pages 배포 | ✅ (선행 완료, 매 단계 push) |
| — | 마무리: 테스트용 버튼 감추기 | ✅ 주석 처리 |

7단계로 계획한 기능은 전부 끝났다. 남은 건 7절의 미해결 항목뿐이다.

### 테스트용 코드 (지금은 주석 처리)
`#devbar` 는 `index.html` 과 `js/main.js` 양쪽에 주석으로 남아 있다 — **둘 다 같이 풀어야**
동작한다(한쪽만 풀면 `getElementById` 가 null 을 준다). `css/ui.css` 의 `#devbar` 규칙은
마크업이 없으면 아무 일도 안 하므로 그대로 뒀다.
`?dev`(시작화면 건너뛰기 + 점수 25815) 와 `?auto=`(자동 클릭) 훅은 살아 있다.

---

## 2. 현재 파일 구조

```
block-game/
├── index.html          무대 · HUD · 보드 · 트레이 · 시작화면
├── manifest.json       PWA (display:fullscreen)
├── css/
│   ├── layout.css      비율 좌표 전부 (원작 실측)
│   └── ui.css          외형 · 애니메이션
├── js/
│   ├── blocks.js       35종 정의 (모양+색 고정). 미관측 4종은 confirmed:false
│   ├── render.js       DOM 생성/갱신. CELL·PITCH 상수(cqw)
│   └── main.js         CONFIG 상수 전부 + Game 모듈
├── assets/             cell_*.png(9) · board.png · bg.png · icon-*.png
├── audio/              mp3 7개
├── tools/
│   ├── bg_gen.py       배경 이미지 생성
│   └── board_crop.py   격자 이미지 크롭
├── docs/analysis.md    원작 분석 전문
└── todo.md             이 파일
```

3단계에서 `js/board.js`(8×8 모델) · `js/drag.js`(포인터 드래그·스냅) ·
`tools/board_test.js`(`node tools/board_test.js`) · `tools/bg_fit.py` 추가.
4단계에서 `js/fx.js`(배너·파편) · `tools/banner_crop.py` 추가.

`Board.countH(lines)` 는 `clearLines` 보다 **먼저** 불러야 한다 (지우고 나면 셀 수 없다).

6단계에서 `js/audio.js` 추가. 볼륨은 `CONFIG.BGM_VOLUME`(0.45) / `SFX_VOLUME`(1.0).
시작 화면 탭에서 `Sound.unlock()` 이 효과음을 무음으로 한 번 틀었다 세운다 —
iOS/사파리는 요소마다 제스처 안에서 재생된 적이 있어야 나중에 코드로 재생된다.

---

## 3. 확정된 규칙 (사용자 결정 사항)

### 게임플레이
- **회전 없음.** 회전 방향마다 별개의 블록으로 취급한다 (그래서 35종)
- 3개 다 못 놓아도 **자동 리필하지 않는다.** 사용자가 block change 로 알아서 푼다
- `block change` **무제한**, 누르면 **1초** 뒤 3개가 새로 채워진다.
  앞 0.5초는 기존 블록이 **줄어들며 사라지고**, 뒤 0.5초는 새 블록이 **커지며 나타난다**
  (`css/ui.css` 의 `trayOut`/`trayIn`. 길이는 `CONFIG.BLOCK_CHANGE_MS` 의 절반과 맞출 것).
  그 1초 동안 모델상 트레이가 비어 아무것도 못 놓는 게 패널티다.
  대기 중에는 버튼이 `disabled`(연타 무시), 카운트다운·종료 중에도 안 먹는다.
  타이머는 계속 흐른다 — 시간 소모가 곧 대가다
- 소모된 슬롯은 즉시 새 블록으로 리필
- 블록 등장 확률은 **35종 균등**. 나중에 가중치로 조정할 수 있게 상수로 둘 것

### 점수
```
획득점수 = 800 × 완성한 줄 수
         + 100 × (완성한 줄에 포함된 h 칸 수)
         +  15 × (이번에 내려놓은 블록의 칸 수)
```
- 가로·세로 동시 완성 시 **두 줄 모두 카운트** (800×2)
- 교차하는 칸은 한 번만 지워지되 **두 줄 모두에 카운트**.
  교차 칸이 h면 **100점을 두 번** 준다
- h 칸은 보드에 놓인 뒤에도 계속 h다. 나중에 다른 블록이 그 줄을 완성해도 100점
- 완성한 줄에 h가 하나라도 있으면 `PERFECT!`, 없으면 `NICE!`

### 드래그 (3단계 핵심)
- **잡은 지점의 상대 위치를 그대로 유지**한다. 손가락 기준 중앙 정렬 같은 건 하지 않는다
- 드롭 판정은 손가락이 아니라 **블록이 실제로 겹쳐 보이는 위치** 기준.
  블록 전체를 통째로 **정수 칸 단위 반올림** (별도 관용 반경 없음, 반올림 자체가 ±반칸)
- **미리보기(반투명 고스트) 없음**
- 무효(범위 밖·겹침)면 **즉시 원위치**, 애니메이션 없음
- 트레이 0.54배 → 집어 올리면 **보드와 1:1(1.0배)**. 그 이상 부풀리지 않는다.
  확대 기준점은 **잡은 지점** (손가락 아래 칸이 안 움직이게)
- 집는 판정은 블록이 아니라 **슬롯 전체**. 1×1을 정확히 찍기 어려워서.
  블록 바깥을 찍었으면 가장 가까운 모서리를 잡은 것으로 친다
- **`EDGE_PULL: 1` — 확정.** 보드 밖으로 1칸까지 삐져나간 블록은 끝 칸으로 밀어 넣는다.
  안쪽 허용치(±반칸)는 건드리지 않는다 — 넓히면 옆 칸 자리를 빼앗아 오배치가 는다.
  겹침은 관용 없음(빈자리 탐색은 검토했다가 **채택 안 함**)

### 타이머
- **95초**, 완전 선형 감소. 게이지는 오른쪽 끝부터 왼쪽으로 줄어든다
- 원작의 time bonus(점수 임계마다 시간 추가)는 **채용하지 않는다**
- 남은 10초부터 경고. 원작은 **화면 전체를 덮는 흰 비네트가 2.2초 주기로 숨쉰다**
  (중앙 타원만 투명). 실측값은 analysis.md 5절.
  시야를 가려서 타원을 1.27배로 넓히고 끝 알파를 0.55 → 0.36 으로 낮췄다.
  `css/ui.css` 의 `#flash` 그라데이션에서 조절

### 연출
- 인트로: `3`(주황) → `2`(청록) → `1`(하늘) → `GO!`(시안). **각 1.0초, 총 4초.**
  움직임 없이 그냥 바뀐다. 영상에서 잘라낸 이미지 (`tools/intro_crop.py`).
  타이머는 `GO!` 가 끝난 뒤 시작한다
- 종료(원작 t=103.60 실측): 화면을 어둡게 덮지 **않는다**. 경고 비네트가 호흡을
  멈추고 65%에서 정지하고, 보드·트레이가 그대로 보이며 화면 정중앙(50%, 49.61%)에
  `GAME OVER` 가 뜬다. 원작은 글자를 타일 **아래**에 깔지만 가려서 안 보인다는
  지적에 따라 맨 앞(z-index 60)으로 올렸다. 앞으로 나오니 원작의 알파 0.5 는
  너무 연해서 `opacity .88 / saturate(1.35)` 로 짙게 했다.
  원작엔 `RETRY` 가 없어서 `block change` 자리를 물려받게 했다
- `TIME BONUS!`: 점수 임계를 넘는 순간 `NICE!`/`PERFECT!` **아래 단**(50%, 30.75%)에
  겹쳐 뜬다. 배너와 같은 0.60초. 영상 26.60초에서 잘라냈다 (`tools/timebonus_crop.py`)
- **이미지 선로딩**: 처음 쓰는 이미지가 그때 디코드되느라 배너가 툭 뜨고 블록의
  마지막 칸이 늦게 채워졌다. `main.js` 의 `preloadImages()` 가 페이지 로드 때
  전부 받아 `decode()` 까지 끝내고 `keep` 배열에 붙들어 둔다 (GC 되면 도로 늦어짐)
- 줄 삭제: 타일이 사라지고 그 자리에서 **다이아몬드 파편 8개가 방사형으로** 튀며
  작아지고 흐려진다. 30fps 기준 2~3프레임(70~100ms)의 아주 짧은 연출
- `NICE!`/`PERFECT!`: **위치 고정** — 무대 중앙 x, 보드 첫 줄 높이(50%, 19.81%).
  지워진 줄 위치와 무관하다. 0.85→1.0배로 커지고 **0.60초 뒤 페이드 없이 사라진다**.
  영상에서 알파까지 잘라낸 워드아트 이미지를 쓴다 (`tools/banner_crop.py`)

### 오디오
| 파일 | 시점 |
|---|---|
| `intro.mp3` | 카운트다운 `3·2·1·GO!` |
| `bgm_loop.mp3` | intro 직후부터 루프, 게임 종료 시 정지 (25.65222초) |
| ~~`sfx_lineclear`~~ | **미사용** — `sfx_nice` 의 일부라서 겹쳐 틀면 두 번 난다 |
| `sfx_nice.mp3` / `sfx_perfect.mp3` | 각 판정 시 |
| `sfx_timebonus.mp3` | 점수가 **6,000 / 9,000** 을 넘는 순간 각 1회 (`TIME BONUS!` 글자 동반) |
| `sfx_gameover.mp3` | 시간 종료 |

- 줄 삭제 시 `lineclear` 와 `nice`/`perfect` 를 **겹쳐서** 재생
- time bonus 는 **효과음 + 글자만**. 실제 시간은 주지 않는다.
  원작대로 `NICE!`/`PERFECT!` 와 **동시에** 뜨고 소리도 겹쳐서 난다
  (겹치지 않게 미뤄 봤다가 원작대로 되돌렸다 — 사용자 결정)
- **`bgm_loop.mp3` 는 효과음을 걷어낸 합성본이다** (`tools/bgm_clean.py`).
  손으로 자른 한 바퀴에는 NICE!/PERFECT!/블록 놓는 소리가 섞여 있었다.
  BGM 은 **1,131,263 표본(25.65222초)마다 똑같이 반복**되고 두 영상에 여덟 바퀴가
  들어 있으므로, 여덟 조각을 표본 단위로 정렬해 놓고 23ms 창마다 **가장 조용한
  조각**을 골라 이었다 (효과음은 더해진 것이라 없는 쪽이 언제나 작다).
  이음매 8곳은 46ms 크로스페이드, 파일 처음과 끝은 **같은 조각**이 덮어 루프가 안 튄다.
  결과: 효과음이 남은 창 1,104개 중 **3개** (원본 각 바퀴는 89~215개).
  > **함정**: 마지막 바퀴들에는 게임오버 뒤 BGM 이 꺼진 무음이 들어 있다.
  > 그냥 최솟값을 고르면 그 무음이 '가장 조용한 조각'으로 뽑혀 루프 끝 3초쯤이
  > 뚝뚝 끊긴다. 그래서 **그 창의 중앙값보다 한참(0.5배) 낮은 조각은 제외**한다.
  > 이걸 안 넣었을 때 창 241개(5.6초)가 무음으로 채워져 있었다.
  > 주기를 25.650 으로 잡았다가 정렬 보정값이 바퀴마다 98표본씩 밀리는 걸 보고
  > 25.65222 로 고쳤다. 고친 뒤엔 보정값이 완전히 일정해졌다. 파일 길이가 곧
  > 루프 길이이므로 이 값이 틀리면 이음매에서 딸꾹거린다.
- `intro.mp3` 는 **두 번째 영상**(`KakaoTalk_20260817_032722265.mp4`)의
  2.300~6.300초를 잘라낸 4.000초짜리다. 첫 영상에서 손으로 자른 wav 에는
  '띠' 가 세 번밖에 없어서(첫 비프가 잘려 나갔다) 한 칸씩 밀렸다.
  두 번째 영상은 네 번이 온전하다 — **2.317 / 3.333 / 4.308 / 5.317(0.63초 긴소리)**,
  1% 문턱에서도 같은 값. 첫 '띠' 17ms 앞에서 잘라 4초를 떴다.
  결과: 음원 안에서 0.040 / 1.050 / 2.025 / **3.045(긴소리)** → 텍스트와 그대로 맞물린다.
  코드 쪽 시각 보정은 없다. 실측: `python tools/intro_beeps.py`
  > 영상의 **텍스트**는 2.15/3.15/4.15/5.15 에 정확히 1.000초 간격으로 바뀐다.
  > 소리가 0.17초 늦는데 이는 화면녹화의 A/V 지연이다. 소리를 기준으로 삼았다.
- 브라우저가 사용자 제스처 전 재생을 막으므로 시작 화면 탭에서 오디오를 깨워야 한다

---

## 4. 좌표 · 배율 (모두 비율)

무대는 1080×2340 비율. 뷰포트에 맞춰 스케일된다. **px 고정 금지.**
`#stage` 가 `container-type:size` 라서 하위 요소는 `cqw`(무대 가로폭의 1%)를 쓸 수 있다.

| 요소 | left | top | width | height |
|---|---|---|---|---|
| 보드(셀 영역) | 10.28% | 17.78% | 79.63% | 36.75% |
| 격자 이미지 | 9.352% | 17.350% | 81.481% | 37.607% |
| 트레이 1/2/3 | 10.28% / 38.15% / 66.02% | 60.56% | 24.44% | 11.54% |
| 점수 알약 | 1.11% | 4.27% | 31.39% | 3.89% |
| 시계 | 61.94% | 4.34% | 8.15% | 3.76% |
| 게이지 | 69.63% | 5.32% | 27.59% | 1.79% |
| block change | 30.09% | 78.12% | 40.28% | 4.27% |

- 보드 내부: 칸 = 보드폭의 11.74%, 간격 0.864%, 피치 12.61%
- `render.js` 상수: `CELL = 9.3519cqw`, `PITCH = 10.0398cqw` (보드 기준 1.0배)
- 트레이는 `CONFIG.TRAY_SCALE = 0.54` 를 곱해 렌더
- HUD 3요소는 세로 중심이 전부 `y=145.5px (6.22%)` 로 같다
- 점수 텍스트는 **자릿수와 무관하게** 알약 폭의 **59.5%** 지점에 중앙 정렬
  (원작에서 1/4/5자리 모두 x=213.75로 실측)

---

## 5. 작업용 명령어

### 배포
```bash
cd "C:/Users/nutel/OneDrive/Desktop/dev/block-game"
git add -A && git -c user.name=yoshi commit -m "..." && git push origin main
```
Pages 가 자동 빌드한다 (1분 내외).

### 렌더 확인 (헤드리스 스크린샷)
```bash
"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu \
  --hide-scrollbars --window-size=540,1170 --screenshot="<경로>/shot.png" \
  --virtual-time-budget=4000 \
  "file:///C:/Users/nutel/OneDrive/Desktop/dev/block-game/index.html?dev"
```
`?dev` 를 붙이면 시작 화면을 건너뛰고 점수를 25815로 채운다 (레이아웃 대조용).
`?auto=fx|end|low|clear|start` 는 0.9초 뒤 해당 버튼을 누른다
(`start` 는 시작 화면을 눌러 카운트다운부터 재생).

> 헤드리스는 짧은 CSS 애니메이션의 **중간 상태를 못 잡는다**. 가상시간 예산이 끝난 뒤
> 실시간이 재개되며 캡처 전에 애니메이션이 끝나버린다. 모양·색·위치만 확인하고
> 움직임은 실기기에서 볼 것. 정 봐야겠으면 duration 을 임시로 9000ms 로 늘린다.

원작과 나란히 비교하려면 `hgame/docs/bg_source.png`(3.6초 지점, 빈 보드) 를 옆에 붙인다.
무대 비율이 같으므로 같은 크기로 리사이즈하면 그대로 겹쳐 볼 수 있다.

### 리소스 재생성
```bash
python tools/bg_fit.py        # 배경 (hgame/bg_source_ai.png 를 1080x2340 에 맞춘다)
python tools/board_crop.py    # 격자 (hgame/docs/bg_source.png 필요)
python tools/banner_crop.py   # NICE! / PERFECT! 워드아트 (영상 필요)
python tools/intro_crop.py    # 카운트다운 3 2 1 GO! (영상 필요)
python tools/timebonus_crop.py # TIME BONUS! 워드아트 (영상 필요)
python tools/intro_beeps.py   # intro.mp3 의 '띠' 위치 실측
python tools/bgm_clean.py     # BGM 루프 (효과음 제거 합성, 영상 2개 필요)
python tools/gameover_alpha.py # GAME OVER 워드아트 (사용자 제공 PNG 필요)
node   tools/board_test.js    # 보드 모델 자체 검증
```
`tools/bg_gen.py` 는 AI 배경을 받기 전에 직접 그리던 것. 이제 안 쓴다.

---

## 6. 함정 · 주의

- **px 고정 금지.** 웹 배포이므로 전부 % 또는 cqw
- 격자 외곽선은 셀 영역 밖으로 4.5px 나간다. 그래서 격자 이미지는 각 변 10px 패딩으로
  자르되 외곽선 바깥은 알파 0으로 잘라낸다 (안 그러면 반투명 사각 링이 보인다)
- 원작 보드는 "칸 + 투명 간격" 이 아니라 **밝은 격자에 어두운 구멍**이 뚫린 한 장이다
- `cell_*.png` 는 101×101, 모서리 라운드 알파. `h` 는 이미지에 이미 그려져 있다
- ffmpeg 는 PATH 에 있으나 **glob 패턴 미지원**. 연속 파일명(`%03d`)을 쓸 것
- Bash 로 파이썬 인라인 코드에 Windows 경로를 넣으면 백슬래시 이스케이프가 깨진다.
  **파이썬 스크립트를 파일로 써서 실행할 것**
- 한글을 셸 명령에 직접 넣으면 깨진다. 커밋 메시지는 heredoc 으로 전달

---

## 7. 미해결 / 나중에 결정할 것

- **폰트**: 원작은 둥근 볼드 산세리프. 지금은 시스템 기본 폰트라 느낌이 다르다.
  웹폰트(Baloo 2 등)를 넣을지, 폰트 파일을 받을지 미정
- **미관측 블록 4종**: 영상에 한 번도 안 나와 색상이 추정치다.
  `blocks.js` 에서 `confirmed: false` 로 표시해 두었다.
  `X./XX`→pink, `XX/X.`→cyan, `.X/XX/X.`→purple, `XX/.X/.X`→pink
- **h 확률 10%**: 체감으로 정한 값. `CONFIG.H_CHANCE` 로 조정
- **최고 점수 저장**: 사용자가 "종료 화면 따로 없다"고 했으므로 일단 넣지 않는다
- **드래그 리프트(`CONFIG.DRAG_LIFT`)**: 지금 0 — 잡은 지점 그대로다(사용자 지시).
  폰에서 손가락에 가려 안 보이면 0.06 정도로 올린다
